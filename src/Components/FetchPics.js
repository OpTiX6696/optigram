import React from "react";
import { Credentials } from "./Credentials";

// import SearchQuery from './Input';


const FetchPics = (props) => {
  // const toSearch = props.inputVal

  console.log(`in fetch pics ${props.inputVal}`);
  return (
    <div>
      Fetch Pictures Components
    </div>
  )
}

export default FetchPics;